services:
  api:
    build: ./api
    restart: unless-stopped

  web:
    build: ./web
    restart: unless-stopped

  proxy:
    build: ./proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - api
      - web